# Feishu app config (required)
FEISHU_APP_ID=cli_xxxxxxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ACP workspace config
ACP_DEFAULT_CWD=/Users/yourname/projects

# Agent switcher config
AGENT_DEFAULT=kimi

# Built-in Kimi provider. KIMI_PATH is kept for backward compatibility.
KIMI_PATH=kimi
AGENT_KIMI_COMMAND=kimi
AGENT_KIMI_ARGS=acp

# Codex provider via Zed's ACP adapter.
# Defaults to gpt-5.5 with high reasoning effort.
AGENT_CODEX_COMMAND=npx
AGENT_CODEX_ARGS=-y @zed-industries/codex-acp -c 'model="gpt-5.5"' -c 'model_reasoning_effort="high"'

# When true, thinking and tool-call content from ACP can be sent to Feishu.
DEBUG=false

# Thinking/tool-call display mode: force (hide), summary, detailed
SHOW_THINKING_TOOL=force

# Optional runtime settings
FEISHU_DOMAIN=feishu
FEISHU_ACK_MODE=reaction
FEISHU_ACK_REACTION=OK
FEISHU_PROCESSING_REACTION=THINKING
# Optional final-status reactions. Leave empty to disable.
FEISHU_DONE_REACTION=
FEISHU_ERROR_REACTION=
FEISHU_SEND_TIMEOUT_MS=15000
FEISHU_IMAGE_MAX_BYTES=10485760
FEISHU_MESSAGE_MERGE_WINDOW_MS=2000
ACP_PROMPT_TIMEOUT_MS=120000
# ACP permission policy: allow_once, allow_always, deny, ask_in_chat
ACP_PERMISSION_MODE=allow_once
# Chat approval timeout when ACP_PERMISSION_MODE=ask_in_chat
ACP_PERMISSION_REQUEST_TIMEOUT_MS=60000
STATE_FILE=.data/state.json
LOG_LEVEL=info

# QQ official bot adapter. Disabled unless explicitly enabled.
QQ_BOT_ENABLED=false
QQ_BOT_APP_ID=
QQ_BOT_APP_SECRET=
# Legacy QQ token auth is still accepted as a fallback, but QQ now recommends Access Token via AppSecret.
QQ_BOT_TOKEN=
QQ_BOT_SANDBOX=false
QQ_BOT_INTENTS=33554432
QQ_BOT_REPLY_MAX_CHARS=1800
QQ_BOT_RECONNECT_MS=5000
QQ_BOT_IMAGE_MAX_BYTES=10485760
QQ_BOT_MESSAGE_MERGE_WINDOW_MS=2000
